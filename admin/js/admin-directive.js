"use strict";

angular.module('AdminDirective', [])
/*
.directive('addresource', function() {
  return {
    restrict: 'E',
    transclude: true,
    scope: {
      resource: '=resource'
    },
    controller: function($scope) {
      $scope.keyDown = function($event){
        console.log($event);
      };
    },
    templateUrl: 'html/add-resource.html',
    link: function(scope, element, attrs){
      console.log(element);
      var input = element.find('#0');
      console.log(input);

      //input.on( "keypress", function(event){
      //  console.log(event);
      //});

    }
  };
});
*/
